@using LearningManagementSystem.Core.Models.Course
@model CourseQueryViewModel

@{
    ViewData["Title"] = "All Courses";
}

<div class="text-center">
    <h1 class="display-4">All Courses</h1>
</div>

<div class="ml-2">
    <a asp-area="" asp-controller="Course" asp-action="Add">
        <input type="button" class="btn btn-outline-primary" value="Add+" />
    </a>
</div>

<div class="container mt-3">
    <div class="row g-4">
        @foreach (var course in Model.Courses)
        {
           <div class="col-12 col-md-6 col-lg-3">
                <div class="card">
                <img src="@course.ImageUrl" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">@course.Title</h5>
                        <p class="card-text">@course.Description</p>
                        @if (User.IsInRole("Student"))
                        {
                            <a class="btn-sm btn-info" asp-controller="Course" asp-action="Details" asp-route-id="@course.Id">Enter & Learn</a>
                        }
                        @if (User.IsInRole("Teacher"))
                        {
                            <a class="btn-sm btn-info" asp-controller="Course" asp-action="Details" asp-route-id="@course.Id">Enter & Edit</a>
                            <a class="btn-sm btn-danger" asp-action="Delete" asp-route-id="@course.Id">Delete</a>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>